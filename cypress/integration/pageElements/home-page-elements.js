// This file contains all Home Page elements

const signInButton = {
    selector: '.login'
};

const logo = {
    selector: '.logo'
};

const homeButton = {
    selector: '.home'
};

export const homePageElements = {
    signInButton,
    logo,
    homeButton
};